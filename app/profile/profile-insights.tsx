"use client";

import type { WeightCohortInsight } from "@/lib/insights";

type Props = {
  weightCohort: WeightCohortInsight | null;
};

/** Kohorta wag lotek — tymczasowo ukryte (za mało danych do sensownego porównania). */
export function ProfileInsights(_props: Props) {
  return null;
}
